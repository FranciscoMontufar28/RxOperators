<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.prueba.francisco.operators">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".subject.PublishSubject">
        </activity>
        <activity android:name=".subject.BehaviorSubject">
        </activity>
        <activity android:name=".FilterOperator">
        </activity>
        <activity android:name=".SkipLastOperator">
        </activity>
        <activity android:name=".SkipOperator">
        </activity>
        <activity android:name=".DistinctOperator">
        </activity>
        <activity android:name=".BufferOperator">
        </activity>
        <activity android:name=".MapOperator">
        </activity>
        <activity android:name=".subject.AsynSubject">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>